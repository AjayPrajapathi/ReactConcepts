import react from 'react'

function Memo() {
    
}












// usememo and use callback both are used for performance optimazation
// useMemo===>in use memo the result of the function is stord in the cache so that if we send same input again it will return the value of input without performing function
// useCallback===> it stores the function instead of result of a function (as in memoization)

